{{-- <form method="GET" action="{{ route('pay_slips.index') }}">
    <input type="text" name="search" placeholder="Rechercher employé, mois ou année..."
           value="{{ request('search') }}">
    <button type="submit">Rechercher</button>
</form> --}}
